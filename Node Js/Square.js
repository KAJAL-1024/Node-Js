cont square={
    area : (a) => (a*a),
    perimeter :(a)=>(4*a)
}

const calsquare=(a)=>{
    console.log('The value of a is ${a} and the area is'
    +square.area(a));
    console.log('The value of a is ${a} and the area is'
    +square.area(a));

}

calsquare(5);